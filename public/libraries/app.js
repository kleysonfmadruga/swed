
window.onload = function () {
    if($('.select2').length > 0) {
        $('.select2').select2({
            dropdownAutoWidth: true,
            width: '100%',
            // disabled: false,
        })
    }
};